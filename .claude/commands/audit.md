Your goal is to update any vulnerable dependencies.

Do the following:
1. Run `npm audit` to find vulnerable dependencies installed packages in this project. 
2. Rum `npm audit fix` to apply updates.
3. Run test and verify the updates din't break anything.